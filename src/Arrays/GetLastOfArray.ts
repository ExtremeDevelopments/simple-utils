/**
 * Get last element of a array
 * @param {Array<any>} arr Array to get last index of
 * @returns {any} Last element of array given
 */
export default function(arr: Array<any>) {
  return arr[arr.length - 1];
}